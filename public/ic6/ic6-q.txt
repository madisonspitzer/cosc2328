1. Name the four layers of the box model from the inside out. Which two add to the total size of the element?
    Content, padding, border, margin 
    Padding and border add to the total size

2. What does margin: 16px auto do, and why does it center the box?
    It adds 16px space on top and bottom, and auto centers the box left to right

3. What is a CSS variable, where do you define it, and how do you use it? Give one example from your file.
    with var(). Example: var(--space)

4. What does the selector .box h2 match, and how is it different from h2 by itself?
        It styles only h2 headings inside .box. h2 by itself would style every h2 on the page